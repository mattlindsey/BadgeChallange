<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @company.name %>
</p>

<%= link_to 'Edit', edit_company_path(@company) %> |
<%= link_to 'Back', companies_path %>
<p>
  
<strong>Badges:</strong>
<%= container_3_tag @company_badges, rows: {cols: {class: 'text-center'}} do |badge| %>
  <h2><%= badge.name + ': ' + @company_badges_tot[badge.id].to_s %></h2>
  <div class="symbol">
    <%= fa_icon badge.fa_symbol %>
  </div>
<% end %>
